import { AppDropdownDirective } from './app-dropdown.directive';

describe('AppDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
